# Pulls Nubi UP's secrets from Bitwarden. Create these Bitwarden items first —
# the app will not start without them:
#   rafael-nubiup-django-secret-key        → Django SECRET_KEY (long random string)
#   rafael-nubiup-db-username / -password  → Postgres user/pass (CNPG bootstrap + app)
#
# Produces two Secrets:
#   nubiup-app-secret → DJANGO_SECRET_KEY
#   nubiup-db-secret  → username, password (CNPG bootstrap reads this too)
#
# Email is deliberately absent. Until the domain is bought there is no mailbox, so
# EMAIL_HOST stays unset and Django uses the console backend (settings/base.py
# picks the backend off EMAIL_HOST). When the mailbox exists, add
# rafael-nubiup-email-host-user / -password here and the matching env in
# deployment.yaml + worker-deployment.yaml. See README.md.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nubiup-app-secret
  namespace: rafael-homelab
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretstore
    kind: ClusterSecretStore
  target:
    name: nubiup-app-secret
    creationPolicy: Owner
  data:
    - secretKey: DJANGO_SECRET_KEY
      remoteRef:
        key: rafael-nubiup-django-secret-key
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nubiup-db-secret
  namespace: rafael-homelab
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-secretstore
    kind: ClusterSecretStore
  target:
    name: nubiup-db-secret
    creationPolicy: Owner
    template:
      data:
        # CNPG requires exactly `username` / `password` for its bootstrap and
        # superuser secrets. Django wants POSTGRES_USER / POSTGRES_PASSWORD, so the
        # same two values are emitted under both names — that lets every workload
        # pull its whole environment with envFrom instead of hand-wiring
        # secretKeyRefs in four places.
        username: "{{ .username }}"
        password: "{{ .password }}"
        POSTGRES_USER: "{{ .username }}"
        POSTGRES_PASSWORD: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: rafael-nubiup-db-username
    - secretKey: password
      remoteRef:
        key: rafael-nubiup-db-password
