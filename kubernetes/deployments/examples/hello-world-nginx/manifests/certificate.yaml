apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: example-webserver-tls-cert
  namespace: rafael-homelab
spec:
  secretName: example-webserver-tls-cert
  issuerRef:
    name: letsencrypt-cloudflare-prod
    kind: ClusterIssuer
  dnsNames:
    - example-webserver.duarte-correia.pt
